# 双模块统计口径

最后复核：2026-08-02

手冲统计从 `hand_brew_records` 推导，做爱统计从 `sex_records` 推导。两类强类型记录只在 UI 层投影为 `DailyCountEntry`，共用同一套纯计算函数；任何统计调用一次只接收一个模块的数据。

## 指标

```text
totalCount = SUM(moduleCount)
occurredDays = COUNT(DISTINCT localDate WHERE moduleCount > 0)
averagePerRecordedDay = totalCount / occurredDays
```

`occurredDays = 0` 时日均显示 `0.0`。`moduleCount = 0` 的日期保留在明细中，但不增加次数和发生天数。未填写没有数据库行；0 次在年度折线中保留基线实心点和数值标签，未填写没有实心点，二者不能混同。

## 周期

- 周：星期一 00:00 对应的本地日期至下周一，不跨周期重复。
- 月：月初至下月月初。
- 年：1 月 1 日至下一年 1 月 1 日。
- 全部历史：最早记录日至今天；没有记录时为空状态。

当前未结束周期显示“进行中”。未来日期和未来月份不生成预测值。

周、月、年都由同一个浏览日期 `anchorDate` 决定。切换模块、日历月份或统计周期不会把它重置为今天。模块选择和周期选择是两个独立状态。

## 展示

- 周视图：固定周一到周日，以七日分布卡逐日直接标注次数和日号；过去未填写显示“未填”，明确 0 次显示 `0`，未来日显示“未来”，TalkBack 仍读出完整日期、次数和天数。
- 月视图：按当前 `YearMonth` 生成 5 或 6 个只包含本月日期的周桶，展示每周次数柱、发生天数、活跃周数和峰值周；不再重复绘制主页日期网格。切换月份必须重新构建周桶，不保留前一月的行或数值。过去未填写、明确 0 次、正次数和未来周使用不同语义，0 次柱高严格为 0。
- 年分析：固定 12 个月直线折线面积图，并列出季度次数占比、最高月份和最低月份。相邻有效月份以直线连接，不使用曲线插值。每月都保留横轴位置；已记录月份显示实心点和次数，明确 0 次落在基线，未填写或未来月份只显示空心位置标记且不参与连线。进入年度统计或切换年度数据时，真实折线、面积、实心节点和次数标签用约 1.6 秒从左向右揭示一次，结束后保持静止且不循环；坐标轴、网格、月份和空心状态点始终稳定显示。未来月份为空，进行中的月份可以展示已发生事实但不进入极值排名；没有合格月份或季度总数为 0 时显示空状态。
- 全部历史：按年份汇总当前模块次数和发生天数。

汇总卡、分布图和表格必须使用相同日期范围；明细加总必须等于汇总。分布图只表达已发生事实，不做预测。

## 固定验收数据

手冲回归数据仍使用 2026 年 1–10 月的 128 次、74 天、日均约 1.7；11–12 月为空。做爱统计使用独立数据集验证 0 次、正次数、未来日期和全部周期，不能读取手冲数据。

## 边界测试

空数据、明确 0 次、同日多次修改、1 月 31 日跨月、2 月 29 日、五周月、六周月、月末、12 月末、跨年周、1970 下限、当前周期上限、未来日期、并列极值、进行中月份、季度总量和设备时区变化。

## UI v2 Stage 4 展示契约

年度分析的历史实现证据见
[`2026-08-01 Stage 4 审计`](product/audit/2026-08-01-ui-v2-stage4/README.md)。该历史审计记录了当时的月度日网格；2026-08-02 的月统计改版契约见
[`MONTH_STATISTICS_REDESIGN.md`](product/MONTH_STATISTICS_REDESIGN.md)，当前运行时以周聚合卡为准。年度在用户 2026-08-02 的定向反馈后改为单张 12 个月折线面积图；只替换趋势表达，统计口径、季度占比和极值规则不变。
