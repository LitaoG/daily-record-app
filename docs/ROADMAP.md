# 版本路线图

路线图使用可验收阶段，而不是固定日期。每个阶段结束时应产生一个可验证结果，并通过 Pull Request 合并。

## Phase 0：需求与开发环境基线

目标：在写业务代码前消除会影响工程结构的关键不确定性。

- 确认应用名称、包名、`minSdk` 和目标平台。
- 检查 Android Studio、Android SDK、内置 JDK、Git 和真机/模拟器。
- 确认主要使用地区与后端候选。
- 确认统计口径和颜色规则。
- 建立 GitHub Issues、Milestones 和基础项目看板。

完成条件：关键决策写入 `DECISIONS.md`，开发机能成功构建官方空白 Compose 项目。

当前进度（2026-07-16）：Android 骨架、CI、产品策略、开源调研、信息架构、KPI 和五张视觉基线已完成。Phase 0 处于验证阶段：先关闭原型颜色语义设计债并执行 5 人可用性测试；Figma 教育权限不阻塞 #5 数据模型准备。

## Phase 1：Android 工程骨架

目标：建立可以长期扩展的最小工程。

- 创建 Kotlin + Compose + Material 3 工程。
- 配置版本目录、代码格式、静态检查和 CI。
- 建立设计系统、导航、依赖注入和错误处理基础。
- 建立 `core`、`feature` 和 `sync` 的边界，早期按需要物理拆分模块。
- 添加单元测试和 Compose 测试样例。

完成条件：Debug APK 可安装；CI 能执行编译、lint 和测试。

## Phase 2：本地 MVP

目标：不登录、不联网也能完成核心记录。

- Room schema、DAO、Repository 和迁移测试。
- 默认活动模板与活动管理。
- 月历、日期详情和快速记录。
- 次数型与完成型活…15049 tokens truncated…用/拒绝清单与许可证边界 |
| 策略 | 定义愿景、目标用户、JTBD、P0/P1/P2 | 云同步后移，本地闭环优先 |
| 语义 | 定义 `UNSET/DONE/MISSED/SKIPPED` | 消除“未记录”和“没做”的混淆 |
| 架构 | 定义 4 个底部导航、页面职责和核心路径 | 月历确定为默认主入口 |
| 指标 | 设计北极星、激活、驱动指标和质量护栏 | 遥测默认不采集活动内容 |
| FigJam | 创建产品发现板和可编辑核心流程 | [打开画布](https://www.figma.com/board/QPalmez5kHyjeaLXeJeZ6y) |
| Canva | 保存修正版产品一页图 | [打开可编辑设计](https://www.canva.com/d/XEOO0XaHd3IjtC_)；已同步汇总胶囊和周/月/年/全部历史统计 |
| 视觉 | 用户选择暖色纸感日志 | 手冲图标冻结为飞机；旧的双点方案废弃 |
| 标记系统 | 对 0/2/4/8+ 活动做压力设计 | 一日期一汇总胶囊、最多 3 色段、完整活动数 |
| 视觉原型 | 生成月历、日期详情、统计、活动管理和组件规范 | [仓库原型入口](PROTOTYPE_V1.md) |
| Figma 原型 | 建立独立可编辑原型文件 | [打开工作区](https://www.figma.com/design/PMtsNNL81BHl9HyJYhjbdw)；教育权限审核中，通过后迁移为原生图层 |
| 指标 | 增加多活动可读性指标 | 4/8+ 汇总数与单活动筛选识别正确率目标均为 90% |
| 统计复核 | 修正单活动筛选混入健身指标的问题 | “手冲”视图只保留手冲指标，并校验月表合计等于年度总次数 |
| 开发准备度 | 冻结 P0 边界、基础类型、依赖候选、实现顺序和 Figma 后续流程 | [开发准备度基线](IMPLEMENTATION_READINESS.md)成为进入本地 MVP 的交接清单 |
| Git 对齐 | 备份本地 `96607db` 并将工作分支安全对齐远端最终 head | 保留 `backup/product-discovery-local-96607db`，未使用 hard reset 或强推 |
| 产品深审 | 审计范围、五张原型、KPI、风险、研究和测试准备度 | 建立 PM 完整性审计、UX 审计、发现研究计划、可用性测试计划和风险登记表 |

## 本轮已冻结的决策

- 先离线 MVP，再账户与跨设备同步。
- 日历是主入口；主导航为日历、统计、活动、设置。
- 状态与数量分离；清除和明确没做不是同一动作。
- 没有目标分母前不展示完成率。
- 归档活动保留历史，P0 不强调永久删除。
- 开源项目只作为产品证据和交互参考，不直接复制 GPL 代码。
- 方向 1“暖色纸感日志”成为视觉基线。
- 手冲在导航外的活动语义中统一使用飞机图标。
- 日历不按活动无限堆点；概览采用单个汇总胶囊，精确信息进入筛选和详情。
- P0 计量类型固定为 `COUNT`、`BOOLEAN`、`DURATION`；任意日期独立颜色、目标、连续天数和提醒不进入 P0。
- Android 实现顺序固定为数据模型 `#5` → 快速记录 `#6` → 日历 `#4` → 统计 `#7`。

## 待用户决策

1. 正式产品展示名称。
2. 首发主要使用地区与未来云端区域。
3. 云端同步进入 P1 后的商业模式与服务成本边界。

## 下一迭代入口

先按[发现研究计划](DISCOVERY_RESEARCH_PLAN.md)和[5 人可用性测试计划](USABILITY_TEST_PLAN.md)验证问题与任务；关闭[原型体验审计](UX_AUDIT_AND_OPTIMIZATION.md)中的阻断项后，再迁移 Figma 原生组件。本地实现从 Issue #5 开始，Figma 不阻塞数据模型准备。
