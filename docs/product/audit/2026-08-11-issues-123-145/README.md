# Issues #123–#145 第三轮代码审计

审计分支：`review/integrate-123-145-gpt5.6luna`  
审计提交：`de3db38`（`[gpt5.6luna]: harden issues 123-145 audit fixes`）  
基线：`origin/main`，不合并到 `main`，等待用户验收。

远端交付：Draft PR [#152](https://github.com/LitaoG/daily-record-app/pull/152)，head `d2117e0`；GitHub Actions Android CI run #241 已通过。PR 保持 Draft，不自动合并到 `main`。

## 结论

本轮逐项复核了 Issues #123–#145 对应的实现、测试和产品文档。没有发现需要推翻已有设计的系统性问题；发现的真实运行时风险已在本分支修复并补回归测试。其余条目属于已实现功能的验收记录、测试债务、文档整理或明确接受的产品取舍，不应伪报为新的运行时缺陷。

## 已修复的真实问题

| Issue | 结论 | 修复与证据 |
|---|---|---|
| #128 | 确认存在 | 坏快照含被拒记录时，AccountSyncManager 不再自动冲刷 pending；Worker 也不继续无意义重试。补充坏快照回归测试。 |
| #130 | 确认存在 | 释放同步互斥锁后再次消费 follow-up 标记，覆盖“循环已消费、解锁前请求到达”的窄竞态。 |
| #131 | 确认存在 | 云端分批删除或账号删除被取消/失败时，以 NonCancellable 尽力把 owner 标回待同步，避免部分清空后永久脱节。 |
| #137 | 确认存在 | 远端 occurrenceIndex 先按有限整数校验再转换；拒绝小数、溢出、重复 index 和重复 detail id，避免 Room 约束异常造成不可收敛失败。 |
| #142 | 确认存在 | CI 去除危险的 `dangerouslyAllowAllBuilds`，改用 pnpm `allowBuilds` 白名单；本地依赖安装和 Firestore 规则测试通过。 |
| #144 | 确认存在 | 1970 首周在真正到达下限时禁用上一页；仍允许从 1970-01-05 的裁剪周回到 1970-01-01。日期轮、密码重置弹窗的触控/滚动问题同步修正。 |

## 已核实、无需新增运行时代码

- #123–#127、#129、#132–#136、#138–#141、#143：实现与现行契约一致，已有测试覆盖；其中 #135 的年度动画只在年度页面进入或年份改变时播放，不做循环播放。
- #134 的周环图标签和图例使用同一状态映射；#136 的逐次详情使用稳定 id，不依赖数组位置。
- #138 的旧日期选择器实现已删除；#139 的死代码/时间边界属于清理项，不影响运行时正确性。
- #140、#141、#143 属于发布脚本、文档和依赖整理，已在现行工程配置中核对。
- 5 秒截止时间仍只作为交互路径契约；后台 Worker 使用自己的后台截止时间和 WorkManager 重试策略。自动同步是否也采用交互截止时间是产品取舍，不在本轮扩大范围。
- 草稿 5→6→5 的 `hasChanges=false` 是“最终值无变化”的现行语义；若未来要求保护所有触碰过的草稿，再单独建立产品决策和测试。

## 验证结果

- 定向 JVM 单元测试：通过（账号删除、同步管理器、远端解析器、日期边界）。
- `compileDebugKotlin`、单元/设备测试编译、`lintDebug`：通过。
- 文档、文案、发布元数据：`1/1`、`2/2`、`4/4` 通过。
- Firestore 规则模拟器：通过。
- Android connected suite：共 129 项，126 通过、2 按设计跳过、3 失败；失败均为测试设备无法连接本机 Firebase Emulator（`10.0.2.2:9099`）的环境超时，失败点在探针连接前，非业务断言失败。其余 UI、Room、同步和账号测试全部通过。
- `git diff --check` 和敏感信息扫描：通过。

## 后续门槛

本分支保持可审查状态，不自动合并到 `main`。用户验收后再决定是否合并；若重新运行设备套件，应先让模拟器能访问宿主机 Firebase Emulator，再把当前 3 个环境失败项补跑。远端 CI 已验证编译、单元测试、Lint、文档/发布元数据和 Firestore rules；设备套件仍需在可访问 Emulator 的环境补跑。
