# 2026-08-02 年度折线与统计页定向验收

状态：`Historical — implementation carried forward into v1.0.0-beta.3`

当时状态（2026-08-02）：`awaiting-user-acceptance`。本页保留年度折线、一次性动画和季度圆环的定向证据；当前运行时以代码和最新公共 `main` 为准。

## 本轮范围

- 删除周、月、年、全部四种统计周期共用的页面级大标题“统计”，筛选器和后续内容随正常布局整体上移；底部导航标签保留。
- 年度次数主图使用相邻有效月份之间的直线连接，不进行曲线插值。
- 进入年度统计或切换年度数据时，仅执行一次约 `1.6s` 的从左向右揭示；结束后不循环。
- 保留 12 个月横轴、动态整数刻度、轻量虚线网格、节点次数标签和模块主题渐变。
- 已记录月份使用实心点；明确 0 位于基线；未填写或未来月份使用空心点、不连线、不预测。
- 延续同一阶段已经完成的季度圆环缩小与日期选择器粒度文案修正。

## 视觉证据

- [用户标注的标题移除参考](title-removal-reference.png)
- [约 0.35 秒时的动画早期帧](animation-early.png)
- [标题移除、布局上移和直线折线最终实现](implementation.png)
- [缩小后的季度圆环](quarter-ring.png)

参考图和实现截图已在同一次视觉比较输入中并排检查。最终实现没有沿用旧的曲线中间态截图，避免审计资产与当前契约冲突。

## 定向验证

- 统计导航设备测试：1 项通过，确认四个周期仍可点击且大标题已删除。
- API 34 模拟器：确认标题消失、筛选器上移、7 月至 8 月为直线、动画早期帧与完成帧存在差异。
- 完成帧与额外等待 `1.2s` 后的空闲帧 SHA-256 完全一致，确认动画不循环。
- `git diff --check`、文档链接检查和敏感信息扫描均通过。

按照 [`TESTING.md`](../../../TESTING.md) 的增量测试规则，本轮不重复运行与这四处定向变化无关的完整设备套件，也不启动 Stage 6、发布或私有镜像同步。
