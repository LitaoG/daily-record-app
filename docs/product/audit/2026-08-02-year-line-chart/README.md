# 2026-08-02 年度折线与季度圆环定向验收

状态：`awaiting-user-acceptance`

## 本轮范围

- 把年度次数主图从 12 月柱状图替换为单张平滑折线面积图。
- 年度图进入页面或切换年度数据时只执行一次从左向右的揭示动画；网格和状态位置保持静止，动画结束后不循环。
- 保留 12 个月横轴、动态整数刻度、轻量虚线网格、节点次数标签和当前模块色渐变。
- 已记录月份使用实心点；明确 0 次位于基线；未填写或未来月份使用空心点、不连线、不预测。
- 季度占比圆环直径由 `118dp` 缩小为 `104dp`，描边由 `24dp` 同比例缩小为 `21dp`。
- 年份选择器按钮显示“跳转到此年”，月份选择器显示“跳转到此月”；日期选择器仍显示“跳转到此日”。

## 视觉证据

- [粗略参考与实现同图对照](comparison.png)
- [手冲年度折线实现](implementation.png)
- [缩小后的季度圆环](quarter-ring.png)

实现没有照搬参考图中把所有空月份画成 0 的演示数据。该差异用于保护既有产品事实：未填写、未来和明确 0 必须保持不同。

## 定向验证

- `:app:compileDebugKotlin`：通过。
- `YearLineChartTest`：通过，覆盖整数刻度、明确 0、未填写和未来月份。
- `YearLineChartTest`：补充揭示进度边界，确保次数标签只在扫描经过对应月份后出现。
- `QuarterShareChartTest`：通过，保留单季度 100% 完整闭环。
- `DateNavigationTest`：通过，覆盖日、月、年三种确认按钮文案。
- API 34 模拟器：完成日历 → 统计 → 年、滚动、手冲/做爱切换、年份弹窗和月份弹窗检查；崩溃缓冲区为空。
- API 34 动效复验：从“月”进入“年”时捕捉到起始揭示帧与完整结束帧；结束后间隔 `1.2s` 的两张截图 SHA-256 完全一致，确认静止且不循环；再次“月 → 年”后重新捕捉到未绘制完成的起始帧。

按 [`TESTING.md`](../../../TESTING.md) 的增量规则，本轮没有重复运行与这些局部改动无关的完整设备套件，也没有启动 Stage 6 或发布流程。
