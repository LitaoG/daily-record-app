# 找回密码与同步可靠性审计

日期：2026-07-22

设备：Pixel 4 API 34 模拟器，1080×2280 物理截图

范围：登录、找回密码、200% 字体、Firebase Auth 模拟器、Room 本地迁入与恢复同步

## 当前运行证据

1. 登录页，正常字号 — **健康**。找回密码入口位于密码框后，登录、注册和本机使用的层级清楚；无裁切或系统默认弹窗。证据：[01-login-api34.png](01-login-api34.png)。
2. 找回密码空态，正常字号 — **健康**。纸感弹窗、邮箱输入、取消和发送操作完整可见；发送在邮箱为空时明确禁用。证据：[02-reset-empty-api34.png](02-reset-empty-api34.png)。
3. 登录页，系统字体 200% — **健康**。内容按页面滚动，邮箱、密码、找回密码和主操作没有横向裁切；系统状态栏字号由 Android 控制。证据：[03-login-font200-api34.png](03-login-font200-api34.png)。
4. 找回密码空态，系统字体 200% — **健康**。标题和说明正常换行，发送与取消纵向排列并全部可见。证据：[04-reset-font200-api34.png](04-reset-font200-api34.png)。
5. 密码重置闭环 — **健康**。隔离 Firebase Auth 模拟器完成注册、发送重置、读取一次性码、设置新密码和新密码登录；未发送真实邮件。
6. 首次登录且 Firestore 不可达 — **健康**。设备测试证明 `__local__` 数据先在 Room 事务中迁入 UID 空间，远端请求失败后账号界面仍可读取原记录。
7. 系统网络状态不变但 Firebase 恢复 — **健康**。设备测试证明实时监听恢复后的新服务器快照会冲刷 `PENDING` 数据，最终进入已同步状态。
8. UID 隔离与规则 — **健康**。Firestore 模拟器拒绝跨账号读取、非法字段、错误修订和物理删除；账号缓存继续按 UID 查询。

## 自动化证据

- JVM：30/30 通过，覆盖密码重置错误映射、统计与日期等纯逻辑。
- Compose：邮箱预填/规范化、格式校验、发送锁、统一成功提示、断网重试和 200% 字体操作可见性。
- Room/同步：本机无网络迁入、离线待同步、服务恢复补偿同步、多设备修改/墓碑、换账号隔离和 v1/v2→v3 迁移。
- Firebase/Android：55 项中 54 项通过、0 失败、1 项生产冒烟按设计跳过；隔离 Auth/Firestore 模拟器覆盖密码重置闭环和所有权规则。
- Firestore 规则：所有权、字段形状、修订和墓碑测试全部通过。
- 构建：Debug、AndroidTest 与 R8 Release 通过；Lint 0 error、7 条仅提示依赖升级的 warning。

## 无障碍证据边界

截图与 Compose 语义断言能够证明文字状态、操作可见性、禁用态、Live Region 和 200% 字体布局。模拟器不能证明真实邮箱客户端的显示、厂商浏览器的重置页、TalkBack 实际朗读音序、触觉反馈或生产邮件送达/垃圾邮件分类；这些项目保留在真机清单中。

## 输出位置

完整审计目录：`C:\Users\86183\Desktop\daily-record-app\docs\product\audit\2026-07-22-password-reset-sync\`
