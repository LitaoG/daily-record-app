# 实现与发布准备度

最后复核：2026-07-28

## 已冻结

- 唯一对象：手冲。
- 每个日期最多一条记录。
- 次数为非负整数；0 表示明确没冲；无记录表示未填写。
- 主导航仅日历与统计。
- 统计周期：周、月、年、全部历史。
- 指标：总次数、手冲天数、记录日均。
- 本地 Room 是唯一 UI 事实来源；邮箱密码账号和云同步可选。
- 登录后本机数据合并到账号；新设备可恢复，普通登录无短信验证码。

## 工程门槛

- Room v3 schema 导出并纳入版本控制。
- v1→v2→v3 与 v2→v3 迁移测试通过，旧手冲数据不丢失。
- 单元测试、Lint、Debug/Release 编译、设备数据库/Compose 和 Firestore 规则测试通过。
- 生产 Firestore 采用 `asia-east1` 生产模式，账户隔离规则已发布。
- Figma 页面不出现活动筛选、健身或活动管理。
- 0 次、未填写、1 次、2 次和 9+ 状态完成视觉与 TalkBack 规范。

## 产品门槛

- 不设置五人测试、任务完成率、中位用时或统计理解率等正式真人测试指标。
- 本人和少量使用者在日常使用中发现的问题进入 GitHub Issue 或后续迭代，不阻塞当前自动化交付。
- 0 次与未填写的区别继续由业务单元测试、界面测试、TalkBack 语义和截图检查验证。
- 发布候选版不得存在已知数据丢失或核心流程崩溃；升级、账号隔离、离线记录和云端恢复门禁必须通过。

## 当前状态

- 当前展示名称固定为“手冲日历”；技术包名保持 `io.github.litaog.dailyrecord`，避免无必要迁移。
- Figma v2、Compose 日历/记录/统计、历史日期跳转、Room v3、邮箱密码登录和云同步基础设施均已实现。
- 当前事实资产只保留在 `assets/hand-brew-v2/`；旧多活动 PNG 已从当前分支删除，仍可通过 Git 历史审计。
- 完整实现、产品文档与验证证据已纳入 Git 提交链，并通过 Pull Request 同步到 GitHub；默认分支落地后以 `main` 为唯一当前事实来源。
- 账号/云数据删除已具备二次确认、密码重验、云端优先删除、本机默认保留/可选删除和失败恢复。
- `v1.0.0-beta.1` 已建立稳定签名、R8 Release、SHA-256、tag 工作流和正式隐私说明；同证书 versionCode 1→2 覆盖安装验证 Room 数据保持。
- App Check 已完成 GitHub 侧载评估；未建立 Play Console 关联前不接入或强制，避免阻断全部侧载用户。
- 当前网络的 Firebase 生产烟雾测试需代理才通过；GitHub Beta 已明确披露这一限制，不承诺中国大陆无代理云恢复，本地优先能力不受影响。
