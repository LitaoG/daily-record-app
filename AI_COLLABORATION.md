# AI 协作记录

> 状态：当前有效
> 最后更新：2026-08-10
> 适用范围：任何参与本仓库开发的 AI 助手。以下条目来自实际协作中被用户指正的问题，不是预设的完整规范；`AGENTS.md` 仍是仓库的正式工程纪律。

## 被指正过的问题

- **提交消息前缀**：用户要求提交消息带 `[ds]:` 前缀（当前约定），提交前先确认。
- **只改公共仓库**：所有代码、分支、PR 在公共仓库进行；私有仓库只用于大版本同步，不要动。
- **合并保留提交历史**：合并候选分支到 `main` 时保留分支里的每一笔提交（用 merge，不用 squash 把多笔提交压成一个）。
- **临时文件用完即删**：临时测试文件、脚本、数据文件用完后立即删除，不要残留。
- **文档/审计改动也要走提交**：改文档或审计内容要在本地分支 commit、push、开 PR，不要在 GitHub 页面直接改。
- **按 Issue 逐步修复**：一次专注一个 Issue，不跳步；改完要有对应测试。
- **文档要与现状一致**：过时的文档要划掉或归档，该改的改，缺失的创建；schema 版本、迁移路径、Issue 状态要随时核对。

## 通用做法

- 流程要走正规：本地验证（测试/Lint/编译）、开 PR、等 CI 通过。
- 动手前不确定的先问用户，涉及不可逆操作（强推、删除、关闭）先确认。
- 修改代码仔细一点，不为了赶进度牺牲质量。
