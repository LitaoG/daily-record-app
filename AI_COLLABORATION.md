# AI 协作记录

> 状态：当前有效
> 最后更新：2026-08-31
> 适用范围：任何参与本仓库开发的 AI 助手。以下条目来自实际协作中被用户指正的问题，不是预设的完整规范；`AGENTS.md` 仍是仓库的正式工程纪律。

## 被指正过的问题

- **改动必须基于分支**：任何代码、文档、测试改动都先从最新 `main` 拉分支（`agent/<名称>`）再做，禁止直接在 `main` 或原分支上改。
- **提交消息前缀**：前缀按"模型名称（或简写）+ 模型型号"写，格式为 `[<模型标识>:<型号>]: <提交内容>`。例如 deepseek-v4-flash 写 `[ds:v4flash]:`，gpt-5.6-sol 写 `[gpt:5.6sol]:`，claude-5 写 `[claude:5]:`。
- **用户指定前缀优先**：如果当前任务明确给出提交前缀，按用户给出的完整字符串执行。本次文档与 README 刷新使用 `[gpt5.6luna]`，提交消息形如 `[gpt5.6luna]: docs: ...`。
- **只改公共仓库**：所有代码、分支、PR 在公共仓库进行；私有仓库只用于大版本同步。
- **合并保留提交历史**：合并候选分支到 `main` 时保留分支里的每一笔提交，不把多笔提交压成一个。
- **临时文件用完即删**：临时测试文件、脚本、数据文件用完后删除，不残留。
- **文档/审计改动也要走提交**：改文档或审计内容在本地分支 commit、push、开 PR，不在 GitHub 页面直接改。
- **先读文档目录**：每次开始任务先读 `docs/DOCUMENTATION_CATALOG.md`，再按任务范围读取其中标记为 AI 必看的当前文档；历史证据只有在核对过去的实现、截图、提交或发布链时才读取。
- **目录与文档同步**：新增、移动、重命名或删除 Markdown/文档时，必须同步更新 `docs/DOCUMENTATION_CATALOG.md`，并运行文档目录完整性测试。
- **严禁批量连发 GitHub Issue/PR 创建请求**：2026-08-16 双语适配（Issue #235 系列）时，AI 在几分钟内连续并并行调用 `gh issue create` 创建多个长篇 Issue，触发 GitHub 滥用/自动化检测，导致用户主账号 `LitaoG` 被暂停、token 失效、远端推送与 Issue 创建全部中断，只能用申诉渠道缓慢恢复。教训：一次会话内创建 Issue 必须极度克制——一次只建一个，间隔至少 60 秒；出现任何一次 403/限流/异常响应后立即停止并如实报告用户，绝不并行重试；需要批量创建 Issue（含长正文）时必须先征得用户明确同意，或由用户在网页端自行创建；推送、PR 等远端写操作同样遵守此节奏，避免把正常开发行为误判为机器人滥用。
- **Issue 正文只写主题目标与步骤**：Issue 只保留主题、目标、范围边界与验收步骤；完整方案、翻译表、长篇论证一律放在仓库文档（`docs/product/<X>_GOAL.md`、审计目录）并在 Issue 正文给文档链接，绝不把全部内容塞进 Issue。2026-08-31 账号解封后补建 Stage 3/4/5 三个 Issue 时沿用此格式，逐个创建且间隔 60 秒以上。

## 通用做法

- 流程走正规：本地验证（测试/Lint/编译）、开 PR、等 CI 通过。
- 按 Issue 逐步修复，一次专注一个；改完带上对应测试。
- 文档要与现状一致：过时的划掉或归档，该改的改，缺失的创建。
- 修改代码仔细一点，不为了赶进度牺牲质量。
