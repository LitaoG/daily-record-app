# 私密日历隐私说明

生效日期：2026-07-26

最后复核：2026-08-08

“私密日历”是一个面向本人和少量朋友的 Android 日历记录工具，分别记录用户主动填写的每日手冲次数和做爱次数。

## 本机数据

- 日历记录、次数和同步状态首先保存在设备上的 Room 数据库。
- 未登录时，核心记录与统计功能完全在本机运行，不要求联网。
- 应用关闭 Android 系统云备份，不把本机数据库交给系统自动迁移。
- 卸载应用通常会删除该设备上的本机数据。

## 可选账号与云同步

用户可以选择 Firebase 邮箱密码账号，用于跨设备恢复。启用后会处理：

- 用户提供的邮箱地址；
- Firebase Authentication 管理的登录凭据与账号标识；
- 用户在两个模块中填写的日期、次数、删除墓碑和同步修订元数据。

应用不收集伴侣姓名或账号、具体时间、地点、时长、姿势、备注、图片、位置或医疗健康信息。

密码由 Firebase Authentication 处理，应用不读取或保存明文密码。密码重置使用 Firebase 官方邮件与托管页面。

云端记录位于登录用户自己的 Firestore 路径，安全规则禁止其他账号读取或写入。普通记录清除使用墓碑防止离线旧设备复活。

## 诊断与分享

应用不会自动上传诊断日志。用户主动打开“诊断”时，可检查、复制或分享一段本机生成的摘要。摘要只包含应用版本、构建类型、设备/Android、同步状态、是否有待同步记录、数据库 schema 和净化后的错误类别，不包含邮箱、UID、具体日期、任一模块次数、密码、token、云端文档或原始异常。

最终分享由用户选择的 Android 应用完成，其后续处理受对应应用的隐私政策约束。

## 数据用途与第三方

数据只用于：

- 展示月历和统计；
- 保存用户主动填写的记录；
- 在用户选择登录后提供同步、换机恢复、密码重置和账号删除。

项目不出售数据，不投放广告，不建立社交画像。云端功能使用 Google Firebase Authentication 和 Cloud Firestore；网络请求及 Firebase 的处理受其服务条款和隐私政策约束。

## 保留与删除

- 用户可清除单日记录；清除会同步墓碑。
- 账号弹窗提供“删除账号与云端数据”，要求二次确认和当前密码重验。
- 删除时先删除本人 Firestore 手冲和做爱记录，再删除 Firebase Authentication 账号。
- 本机记录默认保留为离线记录；用户也可以明确选择同时删除本机记录。
- 网络或权限失败时应用不会谎称删除成功，本机记录仍保留并可重试。

## 网络限制

Firebase 在中国大陆普通网络中可能不可达。用户可以继续完全离线使用；登录、密码重置、同步和云端删除可能需要可访问 Firebase 的网络或 VPN（梯子）。

## 问题反馈

可以通过本仓库 GitHub Issue 报告问题。请优先粘贴应用生成的脱敏诊断摘要，不要公开邮箱、密码、token、具体私密记录日期与次数或其他私人信息。
