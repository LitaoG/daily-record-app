# DeepSeek 协作规范

> 状态：当前有效
> 最后更新：2026-08-10
> 适用范围：所有由 DeepSeek（AI 助手）参与本仓库开发、文档、审查和发布的场景。

本文是用户与 DeepSeek 协作时的**强制约束清单**，防止 AI 偷懒、遗忘或擅自扩大范围。与仓库根目录 [`AGENTS.md`](AGENTS.md) 冲突时，以本文件为准（本文件记录了用户在真实协作中明确提出的更具体要求）。

## 1. Git 与提交纪律

- 所有提交消息必须带 `[ds]:` 前缀，例如 `[ds]: fix: ...`。
- 所有代码、分支、PR 都在**公共仓库** `LitaoG/daily-record-app` 进行；**绝不动私有恢复仓库**（私有仓库只用于大版本同步）。
- 每个任务从最新 `main` 拉短生命周期分支，分支名用 `agent/<描述>`。
- 合并候选分支到 `main` 时，**必须保留分支里的每一笔提交**（使用 `--no-ff` merge 保留完整历史），**禁止 squash 合并**把多笔提交压缩成一个。
- 不强推、不改写共享历史；确需改写历史（如修正错误合并方式）时，必须先用 `--force-with-lease` 且明确告知用户并获得确认。
- 临时测试文件、`.py` 脚本、`.json`/`.txt` 数据文件**用完后必须立即删除**，不得在 `Temp/opencode` 或仓库内残留。
- 用户明确说"不要合并"时，PR 保持 OPEN 等待验收，不擅自合并。

## 2. 流程规范

- 提交 PR 前必须：本地跑受影响范围的定向测试、Lint、AndroidTest 编译、文档完整性脚本（`docs-integrity`、`copy-integrity`、`release-metadata`）。
- PR 必须走 CI（`android-ci.yml` 在 PR 事件触发），CI 通过后才能标记 Ready 或合并。
- PR 标题带 `[ds]:` 前缀和对应 Issue 引用（如 `(#104)`）；PR 正文包含背景、改动清单、验证证据和"不做的事"。
- 修复完一个 Issue 后，按仓库规范同步更新对应 GitHub Issue、路线图、产品契约和必要的 ADR。
- 设备测试只能跑在专用测试模拟器上，不连接日常真机。
- 完成标准：单元测试、Lint、Android 测试编译、Room 迁移测试和全文范围审计证据都通过后才算完成。

## 3. 代码与架构约束

- 产品只有手冲和做爱两个固定独立模块；**不得引入活动表、活动类型、健身或自定义活动抽象**。未来新记录类型必须通过新 ADR 作为独立垂直模块加入。
- 统计口径以 `docs/STATISTICS.md` 为唯一事实来源；`0 次` 与"未填写"必须保持可区分。
- Room schema 变化必须提供显式迁移和迁移设备测试，禁止 destructive migration。
- UI 只通过 Repository 读取 Room（单一事实来源）；不直接访问 DAO 或 Firestore。
- 设备时钟不参与跨设备冲突裁决；冲突由 `remoteRevision` 乐观并发协议决定（ADR-010/017）。
- 删除记录写墓碑；只有账号永久删除流程允许物理删除本人路径。
- 用户可见文案集中在 `AppCopy.kt`；迁移 SQL 中的历史筛选条件必须冻结为字面量，不引用文案常量。
- 修改代码要仔细，**不要为追求效率而牺牲代码质量**；逐步按 Issue 修复，不跳步。

## 4. 文档纪律

- 所有 doc 和 md 文档必须与当前代码事实一致；过时的内容要划掉或标记归档，该修改的修改，缺失的创建。
- 事实文档（PRODUCT/DATA_MODEL/STATISTICS/DECISIONS/ARCHITECTURE）是唯一权威；历史审计文档只解释当时状态，不覆盖当前规则。
- 历史产物（如 `HAND_BREW_REFACTOR_LOG.md`、旧调研、旧设计）必须带明确的"历史/归档"标记，不得与当前文档混淆。
- 新增功能必须先更新产品契约和 ADR，再写代码。
- 文档中 Room schema 版本（当前 v5）、迁移路径（v1→v5 等）、发布版本和 Issue 状态必须随时核对更新。

## 5. 沟通与提问

- 用户是回答者，DeepSeek 是执行者；执行前若发现歧义或冲突，先向用户确认再动手。
- 用户问"你看有没有问题"时，要真实审查并指出问题，不敷衍、不客套。
- 涉及改写历史、删除数据、强推、关闭 Issue 等不可逆操作，必须先征得用户明确同意。
- 不确定用户意图时用提问确认，不要猜。

## 6. 禁止事项

- 禁止把未经验证的测试结果写成"已通过"。
- 禁止在公共仓库提交密钥、签名文件、`google-services.json`、真实用户数据或 APK/AAB。
- 禁止把已关闭的阶段重新当作 `status:in-progress` 执行。
- 禁止在没有新 ADR 的情况下恢复旧通用活动模型或引入万能字段。
- 禁止在用户明确暂停的任务上继续推进（如并发分支冲突时）。
- 禁止删除用户的工作或 stash，除非用户明确要求。
