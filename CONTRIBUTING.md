# 参与贡献

感谢参与 Daily Record。当前项目只记录手冲和做爱两个固定、独立模块的每日次数，优先保证数据正确、离线可用、隐私安全和交互简单；不得为尚未立项的记录类型预先建立通用活动抽象。

## 工作流程

1. 先在 Issue 中说明需求或问题。
2. 从最新 `main` 创建功能分支。所有代码、文档或测试改动都必须在分支上进行，禁止直接在 `main` 上修改。
3. 保持提交聚焦，避免混入无关格式化或重构。
4. 新功能补充测试和相关文档。
5. 创建 Pull Request，并填写模板中的验证结果。

## 分支与提交

- 功能：`feature/<name>`
- 修复：`fix/<name>`
- 文档：`docs/<name>`
- 自动化代理：`agent/<name>`

提交信息使用简短祈使句，例如：

- `Add hand-brew calendar states`
- `Define monthly brew statistics`
- `Fix week boundary calculation`

## 代码要求

- Kotlin 代码遵循官方 Kotlin 风格。
- Compose UI 使用状态提升和单向数据流。
- Repository 是 UI/领域层访问数据的唯一入口。
- 日期计算显式使用 `LocalDate`；周固定从星期一开始。
- 当前功能不引入 `Activity`、`MeasurementType` 或活动筛选。未来记录类型需要先有产品契约和 ADR，并作为独立垂直模块实现，不能污染现有手冲或做爱领域模型。
- 不允许用数据库清空重建代替正式迁移。

## 测试要求

测试范围和执行时机以 [`docs/TESTING.md`](docs/TESTING.md) 为准：日常修改运行影响范围内的定向测试，一个连贯小版本只在最终功能 head 上执行一次完整套件；未受后续修改影响的证据可以沿用。

- 统计公式、日期边界、0 次与未填写语义需要单元测试。
- Room schema 变化需要迁移测试。
- 关键用户流程需要 Compose UI 测试。
- PR 中列出已运行的命令和结果。
- 完整 Android 设备套件只在可重置的测试模拟器上运行，不连接保存日常数据的真机。

## 隐私与示例数据

测试和截图只能使用虚构数据。请勿提交：

- 真实行为记录、备注或账号信息。
- `local.properties`、`.env`、服务端密钥。
- 签名文件、密码、访问令牌。
- 用户数据库、备份文件或包含隐私的日志。
